language: java

jdk:
  - oraclejdk8

os:
  - linux

branches:
  only:
    - master

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

services: mongodb

script:
  - ./gradlew build -x integrationTest

after_success:
  - codecov
