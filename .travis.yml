language: java

jdk:
  - oraclejdk8

os:
  - linux

branches:
  only:
    - master

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

services: mongodb

script:
  - ./gradlew build codeCoverageReport -x integrationTest

after_success:
  - bash <(curl -s https://codecov.io/bash)